import AppShell from "@/components/AppShell";

export const metadata = {
  title: "Attorney Network | ImmFlow",
  description: "Connect with immigration attorneys for co-counsel, hearing coverage, and referrals.",
};

export default function NetworkRoute() {
  return <AppShell initialPage="network" />;
}
